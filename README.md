# База пользователей

Приложение для управления базой пользователей, разработанное с использованием React, TypeScript и архитектуры FSD.

## 🚀 Технологии

- **TypeScript** - типизированный JavaScript
- **React 16** - библиотека для создания пользовательских интерфейсов
- **Webpack** - сборщик модулей
- **Ant Design 5** - UI библиотека компонентов
- **styled-components** - CSS-in-JS решение для стилизации
- **React Router 6** - маршрутизация
- **TanStack Query 4** - управление состоянием сервера
- **axios** - HTTP клиент
- **dayjs** - библиотека для работы с датами

## 📁 Архитектура

Проект использует архитектуру **FSD (Feature-Sliced Design)**:

```
src/
├── app/           # Конфигурация приложения
├── pages/         # Страницы приложения
├── features/      # Бизнес-функции
├── shared/        # Переиспользуемые компоненты и утилиты
│   ├── api/       # API конфигурация и запросы
│   ├── hooks/     # Переиспользуемые хуки
│   ├── types/     # TypeScript типы
│   └── ui/        # UI компоненты
└── index.tsx      # Точка входа
```

## 🛠 Установка и запуск

### Предварительные требования
- Node.js (версия 14 или выше)
- npm или yarn

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm start
```

Приложение будет доступно по адресу: http://localhost:3000

### Сборка для продакшена
```bash
npm run build
```

## 🔐 Авторизация

Для входа в систему используйте:
- **Логин:** admin
- **Пароль:** admin

## 📋 Функциональность

### Страницы
- **Авторизация** (`/login`) - вход в систему
- **Пользователи** (`/users`) - управление пользователями
- **404** - страница для несуществующих маршрутов

### Возможности
- ✅ Авторизация с проверкой токена
- ✅ Просмотр списка пользователей
- ✅ Создание новых пользователей
- ✅ Редактирование существующих пользователей
- ✅ Форматирование дат (DD.MM.YYYY)
- ✅ Валидация форм
- ✅ Обработка ошибок
- ✅ Адаптивный дизайн

## 🌐 API

Приложение использует MockAPI для имитации backend:
- **Base URL:** https://696b8df8624d7ddccaa183fb.mockapi.io/api/v1
- **Endpoint:** `/users`

### Структура пользователя
```typescript
interface User {
  id: string;
  createdAt: string;
  name: string;
  avatar: string;
  email?: string;
}
```

## 🔄 Workflow разработки

Проект разрабатывался поэтапно с использованием Git веток:

1. **main** - основная ветка
2. **feature/auth-page** - страница авторизации
3. **feature/users-page** - страница пользователей
4. **feature/final-polish** - финальная полировка

## 📝 Скрипты

- `npm start` - запуск dev сервера
- `npm run build` - сборка для продакшена
- `npm run dev` - запуск dev сервера с автооткрытием браузера

## 🎯 Особенности реализации

- **Защищенные маршруты** - доступ к `/users` только после авторизации
- **Автоматическая проверка авторизации** при обновлении страницы
- **Кеширование данных** с помощью TanStack Query
- **Оптимистичные обновления** при создании/редактировании пользователей
- **Обработка состояний загрузки** и ошибок
- **Блокировка UI** во время выполнения запросов

## 🚀 Демо

Приложение развернуто и доступно по ссылке: [GitHub Repository](https://github.com/mukhacheva01/internship)

---

Разработано с ❤️ для изучения современных веб-технологий